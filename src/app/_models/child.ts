export class Child {
    names: string
    gender: string
    dob: string

    constructor(names: string, gender: string, dob: string) {
        this.names = names
        this.gender = gender
        this.dob = dob
    }
}
